---
import Layout from "../layouts/Layout.astro";
import AgentLanding from "../components/AgentLanding.astro";
import Walkthrough from "../components/Walkthrough.astro";
import HeroCanvas from "../components/HeroCanvas.astro";
import Stats from "../components/Stats.astro";
import { site } from "../lib/site";

const roles = [
  "fine-tune small LLMs",
  "build agentic systems",
  "design LLM evaluations",
  "ship production ML",
];

const skillGroups = [
  {
    label: "LLMs & Agentic Systems",
    items: [
      "LLM Fine-Tuning (SFT & RL)",
      "LangChain / LangGraph",
      "Agentic RAG & Tool Calling",
      "Multi-Agent Orchestration",
      "LLM-as-Judge Evaluation",
    ],
  },
  {
    label: "Search, Retrieval & Data",
    items: [
      "FAISS / Qdrant / Pinecone",
      "Hybrid Retrieval (BM25 + Embeddings)",
      "Re-ranking Models",
      "Golden Datasets & Offline Eval",
    ],
  },
  {
    label: "Backend & Infra",
    items: [
      "Python (FastAPI)",
      "Microservices",
      "Docker / Kubernetes",
      "Observability-First Design",
    ],
  },
  {
    label: "ML & DL",
    items: [
      "Supervised Learning & Embeddings",
      "Ranking / Learning-to-Rank",
      "Model Evaluation & Calibration",
      "Experimentation Frameworks",
    ],
  },
];
---

<Layout>
  <AgentLanding />
  <div class="agent-gate" id="main-content">
  <section class="relative overflow-hidden border-b border-neutral-200 dark:border-white/10">
    <HeroCanvas />
    <div class="pointer-events-none absolute inset-0 bg-gradient-to-b from-white/40 via-white/70 to-white dark:from-neutral-950/40 dark:via-neutral-950/70 dark:to-neutral-950"></div>

    <div class="relative mx-auto max-w-5xl px-6 pb-20 pt-24 sm:pt-32">
      <p data-reveal class="font-mono text-sm text-emerald-600 dark:text-emerald-400">Hi, I'm</p>
      <h1 data-reveal style="--reveal-delay:80ms" class="mt-2 text-4xl font-bold tracking-tight text-neutral-900 dark:text-neutral-50 sm:text-6xl">
        {site.name}
      </h1>
      <h2 data-reveal style="--reveal-delay:160ms" class="mt-4 text-xl text-neutral-600 dark:text-neutral-300 sm:text-2xl">
        I <span id="typed-role" class="type-caret text-gradient font-semibold"></span>
      </h2>
      <p data-reveal style="--reveal-delay:240ms" class="mt-6 max-w-2xl text-base leading-relaxed text-neutral-600 dark:text-neutral-400 sm:text-lg">
        I take messy, ambiguous problems and turn them into production-grade AI systems that
        ship, end to end: from data and evaluation design to fine-tuning, agent orchestration,
        and backend integration. Currently {site.currentRole.title} at {site.currentRole.company}.
      </p>

      <div data-reveal style="--reveal-delay:320ms" class="mt-8 flex flex-wrap gap-4">
        <a
          href="/projects"
          class="rounded-md bg-emerald-500 px-5 py-2.5 text-sm font-medium text-neutral-950 transition hover:bg-emerald-400 hover:shadow-lg hover:shadow-emerald-500/20"
        >
          View Projects
        </a>
        <a
          href="/contact"
          class="rounded-md border border-neutral-300 px-5 py-2.5 text-sm font-medium text-neutral-700 transition hover:border-emerald-500/60 hover:text-emerald-600 dark:border-white/15 dark:text-neutral-200 dark:hover:border-emerald-400/50 dark:hover:text-emerald-300"
        >
          Get in Touch
        </a>
      </div>
    </div>
  </section>

  <Walkthrough />

  <Stats />

  <section class="mx-auto max-w-5xl px-6 py-14">
    <h3 data-reveal class="text-lg font-semibold text-neutral-900 dark:text-neutral-100">Core Skills</h3>
    <div class="mt-6 grid gap-6 sm:grid-cols-2">
      {skillGroups.map((group, i) => (
        <div
          data-reveal
          style={`--reveal-delay:${i * 90}ms`}
          class="glow-card rounded-lg border border-neutral-200 bg-neutral-50 p-5 dark:border-white/10 dark:bg-white/[0.02]"
        >
          <p class="font-mono text-xs uppercase tracking-wide text-neutral-500">{group.label}</p>
          <ul class="mt-3 flex flex-wrap gap-2">
            {group.items.map((item) => (
              <li class="rounded-full border border-neutral-200 px-3 py-1 text-xs text-neutral-700 transition hover:border-emerald-500/40 hover:text-emerald-600 dark:border-white/10 dark:text-neutral-300 dark:hover:border-emerald-400/40 dark:hover:text-emerald-300">
                {item}
              </li>
            ))}
          </ul>
        </div>
      ))}
    </div>
  </section>
  </div>
</Layout>

<script define:vars={{ roles }}>
  const typeRoles = () => {
    const el = document.getElementById("typed-role");
    if (!el) return;
    const reduced = window.matchMedia("(prefers-reduced-motion: reduce)").matches;
    if (reduced) {
      el.textContent = roles[0];
      return;
    }

    let roleIdx = 0;
    let charIdx = 0;
    let deleting = false;

    const tick = () => {
      const current = roles[roleIdx];
      if (!deleting) {
        charIdx++;
        el.textContent = current.slice(0, charIdx);
        if (charIdx === current.length) {
          deleting = true;
          setTimeout(tick, 1600);
          return;
        }
        setTimeout(tick, 55);
      } else {
        charIdx--;
        el.textContent = current.slice(0, charIdx);
        if (charIdx === 0) {
          deleting = false;
          roleIdx = (roleIdx + 1) % roles.length;
          setTimeout(tick, 300);
          return;
        }
        setTimeout(tick, 30);
      }
    };
    tick();
  };

  typeRoles();
  document.addEventListener("astro:page-load", typeRoles);
</script>
